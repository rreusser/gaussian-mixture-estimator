"use strict";

module.exports = outerProduct;

// Compute the outer product: c <- a * b^T
function outerProduct (c, a, b) {
  var M = a.shape[0];
  var N = b.shape[0];

  for (var i = 0; i < M; i++) {

  }
}
